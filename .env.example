# Server Configuration
PORT=5000
NODE_ENV=development
JWT_SECRET=antigravity-secret-key-2026-production

# PostgreSQL Database URL (Optional if using PostgreSQL service)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/lastmin
